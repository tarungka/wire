interface:
  display_name: "Wire"
  short_description: "Repo-specific patterns and workflows for wire"
  default_prompt: "Use the wire repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true